API_BASE_URL=/api/v1
# On Vercel, set this in Project Settings > Environment Variables before building:
# API_BASE_URL=https://your-public-backend.example/api/v1
# Redeploy after changing it. The Vite proxy below does not run on Vercel.
# Used by the development and preview proxy; not exposed to the browser.
API_PROXY_TARGET=http://localhost:8000
